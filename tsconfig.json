{
	"compilerOptions": {
		"module": "commonjs",
		"target": "ES2020",
		"lib": [
			"ES2020",
			// This is needed for ix - library with async iterable operators
			"DOM"
		],
		"outDir": "out/src",
		"sourceMap": true,
		"rootDir": "src",
		"strict": true,
		"noImplicitReturns": true,
		"noFallthroughCasesInSwitch": true,
		"baseUrl": "src",
	},
	"include": [
		"src/",
		// eslint is giving me a hard time because it wants to reference a single project configuration, but we have a composite project
		// "configurations/"
	],
	// We have a dependency typescript package that creates a language server bundle that needs to be rebuild when we are creating the extension
	// https://www.typescriptlang.org/docs/handbook/project-references.html#what-is-a-project-reference
	"references": [
		{
			"path": "lsp-task-expression"
		}
	]
}