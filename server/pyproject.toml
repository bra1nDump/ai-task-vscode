[tool.poetry]
name = "server"
version = "0.1.0"
description = ""
authors = ["Kirill Dubovitskiy <kirill2003de@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
langchain = "^0.0.267"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
